# AuthraGen Python SDK

Vendor-neutral agent identity, exact-intent authorization, delegation, approvals, revocation and offline credential verification.

See the repository README and PROTOCOL.md for the wire protocol and deployment guidance.
